query getCategory($id: ID!) {
  category(id: $id) {
    id
    name
    rankable
    videos {
      id
      title
      duration {
        minutes
        seconds
      }
      landscapeThumbnail
    }
  }
}
